{
    "version": 2,
    "framework": "vite",
    "rewrites": [{ "source": "/(.*)", "destination": "/" }],
    "buildCommand": "pnpm build",
    "outputDirectory": "dist",
    "devCommand": "pnpm dev",
    "installCommand": "pnpm install"
  }